import version, { double, pow } from './helpers/numbers.mjs'
// получаем дефортное значение, обращаясь не к обьекту а к единичной переменной
// название - любое, вне зависимост иот названия в том файле
 
import { random } from 'lodash-es'

console.log(double(4), pow)
console.log(version)
console.log(random())